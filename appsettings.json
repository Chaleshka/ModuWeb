{
  "BaseApiPath": "api/",
  "BaseDbPath": "db/",
  "MaxRequestBodySize": 50, //In MB
  "Url": "http://*:5000",
  "UseHttps": false, //If true, specify Certificate below
  "Certificate": { //Used only when UseHttps is true. If you dont need Certificate, just delete it
    "Path": "cert/cert.pem",
    "KeyPath": "cert/key.pem"
  },
  "LoadOrder": [ //The order of the modules that will be loaded first and in the following order

  ]
}
